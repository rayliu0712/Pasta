<Window
  x:Class="Pasta.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Pasta"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="Pasta"
  Width="250"
  Background="#1E1E1E"
  Icon="icon.ico"
  MouseLeftButtonDown="Window_MouseLeftButtonDown"
  ResizeMode="CanMinimize"
  SizeToContent="Height"
  Topmost="True"
  mc:Ignorable="d">

  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Background" Value="#333333" />
      <Setter Property="Foreground" Value="#FFFFFF" />
      <Setter Property="BorderBrush" Value="#555555" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border
              x:Name="border"
              Padding="{TemplateBinding Padding}"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="1"
              CornerRadius="3">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#444444" />
        </Trigger>
        <Trigger Property="IsPressed" Value="True">
          <Setter Property="Background" Value="#555555" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style
      x:Key="PrimaryButton"
      BasedOn="{StaticResource {x:Type Button}}"
      TargetType="Button">
      <Setter Property="Background" Value="#0078D4" />
      <Setter Property="BorderBrush" Value="#0078D4" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#1A86D8" />
          <Setter Property="BorderBrush" Value="#1A86D8" />
        </Trigger>
        <Trigger Property="IsPressed" Value="True">
          <Setter Property="Background" Value="#006CBE" />
          <Setter Property="BorderBrush" Value="#006CBE" />
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <DockPanel>
    <TextBlock
      Margin="0,0,0,12"
      HorizontalAlignment="Center"
      DockPanel.Dock="Bottom">
      <Hyperlink
        Foreground="#888888"
        NavigateUri="https://github.com/rayliu0712/Pasta"
        RequestNavigate="Hyperlink_RequestNavigate">
        GitHub
      </Hyperlink>
    </TextBlock>
    <StackPanel
      Margin="24"
      HorizontalAlignment="Center"
      VerticalAlignment="Center">
      <Button
        x:Name="OpenButton"
        MinWidth="120"
        Margin="0,0,0,12"
        Padding="32,10"
        Click="OpenButton_Click">
        Open
      </Button>
      <Button
        x:Name="PasteButton"
        MinWidth="120"
        Padding="32,10"
        Click="PasteButton_Click"
        Style="{StaticResource PrimaryButton}">
        Paste
      </Button>
      <TextBlock
        x:Name="ErrorText"
        Margin="0,12,0,0"
        HorizontalAlignment="Center"
        Foreground="#FF4444"
        TextWrapping="Wrap"
        Visibility="Collapsed" />
    </StackPanel>
  </DockPanel>
</Window>
